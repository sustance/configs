TRAVEL Next Trips Book

All pdfs for a single name prefix (trip) for the first day af travel on that trip

Get PDFs of all ticket and in target folder with names YYYMMDDi-* where "i" sets sequence within date.

run: incpdfTravel.lua 20241210 YYMMDD <-- for trips to include in this book

Script 1-*.sh
Cut and paste that output to NextTickets.tex NOTE This can be automated but this should enable the book to be recreated without keeping a cpy)

Compile NextTickets.tex with LuaTeX NOTE this will give a random number of pages and must be still be adjusted to fit 8 page signatures

Script 2-*.sh
Compile signaturesX8.tex
Script 3-*.sh
